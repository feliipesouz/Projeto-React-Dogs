import React from "react";
import Feed from "../Feed";
import "../../App.css";

const Home = () => {
  return (
    <section className="container mainContainer">
      <Feed />
    </section>
  );
};

export default Home;
