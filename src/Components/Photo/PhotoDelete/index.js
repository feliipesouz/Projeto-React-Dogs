import React from "react";
import { ButtonDelete } from "./styles";

const PhotoDelete = ({ id }) => {
  function onclick() {}

  return (
    <div>
      <ButtonDelete onClick={onclick}>Delete</ButtonDelete>
    </div>
  );
};

export default PhotoDelete;
